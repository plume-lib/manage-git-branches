name: Tests
"on": [push, pull_request]
permissions:
  contents: read
jobs:
  tests:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v7
      - run: tests/run-tests.sh
      - run: tests/env-vars-documented.sh
