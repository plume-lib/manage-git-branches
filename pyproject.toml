[project]
name = "manage-git-branches"
version = "0.1.0"
description = "Scripts to support managing multiple branches of a git repository."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[dependency-groups]
dev = ["pymarkdownlnt>=0.9.33", "ty>=0.0.1a32"]
